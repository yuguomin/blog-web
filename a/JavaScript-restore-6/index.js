<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css">window.alert(1);
document.addEventListener('ready', () => {
  const loadEle = document.querySelector('.load');
  const addEle = document.querySelector('.add');
  const countEle = document.querySelector('.count');

  let count = 0;
  countEle.innerText = count;
  loadEle.addEventListener('click', () => {
    let index = 0;
    while (index <= 4000000000) { if (index="==" 0) console.log('running'); } 1000000000) console.log('xxx.js is ok;'); 2200000000) console.log('zzz.css console.log('ooo.png status="true;" index++; }); addele.addeventlistener('click', ()> {
    count += 1;
    countEle.innerText = count;
  });
});</=>